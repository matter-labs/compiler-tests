//! { "cases": [ {
//!     "entry": "entry",
//!     "expected": 50
//! } ] }

const SIZE_HALVED: u8 = 2;

fn entry() -> u64 {
    main(42) as u64
}

fn main(mut value: u8) -> u8 {
    let array = [2; SIZE_HALVED * 2];
    for i in 0..SIZE_HALVED * 2 {
        value += array[i];
    }
    value
}
