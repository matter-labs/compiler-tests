//! { "cases": [ {
//!     "entry": "entry",
//!     "expected": 48
//! } ] }

enum List {
    FIRST = 1,
    SECOND = 2,
    THIRD = 3,
}

fn entry() -> u64 {
    main(8) as u64
}

impl List {
    pub fn multiply_by_all(self) -> u256 {
        self as u256 * Self::FIRST as u256 * Self::SECOND as u256 * Self::THIRD as u256
    }
}

fn main(witness: u8) -> u256 {
    List::FIRST.multiply_by_all() * (witness as u256)
}
