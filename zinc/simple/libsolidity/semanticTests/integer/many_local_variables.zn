//! { "cases": [ {
//!     "entry": "entry",
//!     "expected": 1184033
//! } ] }

fn entry() -> u64 {
    run(0x1000, 0x10000, 0x100000) as u64
}

fn run(x1: u256, x2: u256, x3: u256) -> u256 {
    let a = 1; let b = 16; let c = 256 as u16;
    let mut y = a as u256 + b as u256 + c as u256 + x1 + x2 + x3;
    y += b as u256 + x2;
    y
}