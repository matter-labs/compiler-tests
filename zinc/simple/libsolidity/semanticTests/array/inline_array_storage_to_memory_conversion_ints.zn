//! { "cases": [ {
//!     "entry": "entry",
//!     "expected": 1
//! } ] }

fn entry() -> u64 {
    let output = f();
    if output.0 == 3 && output.1 == 6
        {1} else {0}
}

fn f() -> (u256, u256) {
    let x = 3 as u256;
    let y = 6 as u256;
    let z = [x, y];
    (z[0], z[1])
}