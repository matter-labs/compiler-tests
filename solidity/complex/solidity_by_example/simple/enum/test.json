{ "cases": [ {
    "name": "default",
    "input": [
        {
            "instance": "Enum",
            "entry": "status",
            "calldata": [
            ]
        }
    ],
    "expected": [
        "0"
    ]
} ],
    "contracts": {
        "Enum": "main.sol:Enum"
    }
}